[package]
name = "app"
version = "0.1.0"
edition = "2024"

[dependencies]
common = { path = "../common" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# allow me to use cargo build --features 40tude + cargo run -p app --features 40tude
# cargo run -p app --features 40tude
[features]
40tude = []
