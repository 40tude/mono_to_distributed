[package]
name = "component1_lib"
version = "0.1.0"
edition = "2024"

[lib]
# This creates a dynamic library (.dll on Windows, .so on Linux, .dylib on macOS)
# crate-type = ["cdylib", "rlib"]

[dependencies]
traits = { path = "../traits" }
